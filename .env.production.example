# Set this in Vercel Project Settings > Environment Variables before building.
# Use the deployed ASP.NET Core API origin, without a trailing slash.
NEXT_PUBLIC_API_BASE_URL=https://your-backend-api-domain
